export function LoadingSpectrum() {
  return (
    <div className="relative h-full min-h-[calc(100vh-80px)] w-full">
      <div className="absolute inset-0 bg-gradient-to-r from-blue-400 via-gray-100 to-red-400 animate-pulse" />
    </div>
  )
} 