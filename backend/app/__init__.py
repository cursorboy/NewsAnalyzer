"""FastAPI application package for the backend service.

This file ensures that the `app` directory is treated as a Python package,
which makes imports like `app.main:app` work reliably when running the server.
"""


